name: sigbo_alertas
description: Alertas inmediatas de bomberos CBVC (apoyo / chofer). Cliente movil del backend SIGBO existente.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.2
  flutter_secure_storage: ^9.2.2
  flutter_local_notifications: ^17.2.3
  permission_handler: ^11.3.1
  workmanager: ^0.10.10
  flutter_foreground_task: ^8.11.2
  flutter_ringtone_player: ^4.0.0
  vibration: ^2.1.0
  file_picker: ^8.1.2
  uuid: ^4.5.1
  geolocator: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
